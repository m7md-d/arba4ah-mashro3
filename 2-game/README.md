# 2-game

Planned: a real-time multiplayer shooting game played entirely over a raw TCP connection.

The client is just a plain terminal (`nc`) — no dedicated client app. The server continuously pushes updates to redraw the client's terminal in real time as the game state changes.

Not implemented yet.
